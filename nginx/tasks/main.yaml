---

- name: install nginx
  import_task: install.yaml

- name : start nginx
  service:
    name: nginx
    state: started
  become: true

- name: Copier la configuration Nginx
  ansible.builtin.template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf
  notify: restart nginx

