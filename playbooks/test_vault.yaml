- name: Return all secrets from a path
  hosts: localhost
  tasks:
    - name : Print secret db test
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=secret/data/myapp token={{ vault_token }} url=https://hcpvault.nelixair.com') }}"