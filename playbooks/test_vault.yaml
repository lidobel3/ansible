- name: Return all secrets from a path
  hosts: localhost
  tasks:
    - name : Print secret db test
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=secret/prod/proxmox/data/my-secret token={{ vault_token }} url=https://hcpvault.nelixair.com') }}"