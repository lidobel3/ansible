- name: Return all secrets from a path
  hosts: localhost
  tasks:
    - name : Print secret db test
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=secret/db token='s=.vv/dSJ&V2$xVuq|:%n{7j' url=https://hcpvault.nelixair.com') }}"