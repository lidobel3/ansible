---

- name: check status
  import_tasks: check_status.yml

# Impossible à utiliser tant que l'IP n'aura pas été rajoutée au SAN du certificat
# - name: login to hashicorp vault server
#   community.hashi_vault.vault_login:
#     url: "{{ vault_addr }}"
#     token: "{{ vault_token }}"
#   register: vault_status
#   delegate_to: localhost
#   run_once: true
#   become: false
#   ignore_errors: true

# - name: print result
#   debug:
#     msg: vault_status

- name: Installation des pre-requis
  import_tasks: prerequis.yml
  # when: vault_status.rc != 0 and primo_install_vault

- name: Déploiement et configuration de Vault
  import_tasks: deploy.yml
  # when: vault_status.rc != 0  

- name: Test de l'installation de Vault
  import_tasks: check_status.yml
  # when: vault_status.rc != 0
  
# - name: Générer et appliquer la politique
#   community.hashi_vault.vault_policy:
#     url: "{{ vault_addr }}"
#     token: "{{ vault_root_token }}"
#     name: "policy-{{ app_name }}"
#     # On utilise lookup pour transformer le template en texte
#     rules: "{{ lookup('template', 'app-policy.hcl.j2') }}"
#     state: present
# Je n'arrive pas à faire fonctionner la collection community.hashi_vault.vault_policy