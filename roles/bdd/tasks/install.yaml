- name: Installer PostgreSQL
  apt:
    name: "postgresql-{{ postgres_version }}"
    state: present
    update_cache: yes
  become: true 

- name: Démarrer et activer PostgreSQL
  service:
    name: postgresql
    state: started
    enabled: true
  become: true

